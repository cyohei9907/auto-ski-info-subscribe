# 快速开始指南# X.com 自动登录爬虫 - 快速开始

## 🚀 5 分钟快速部署## 🚀 3 分钟快速设置

本指南帮助您快速部署 **Auto Ski Info Subscribe** 系统。### 方法 1: 直接运行脚本（推荐）

---```bash

# 在 Windows 本地运行

## 📋 准备工作 cd backend

python local_setup_auth.py

### 必需项```

- ✅ Docker 和 Docker Compose（[安装教程](https://docs.docker.com/get-docker/)）

- ✅ X（Twitter）账号及其 Cookie**脚本会：**

- ✅ Google Gemini API Key（可选，用于 AI 分析）

1. ✅ 提示你输入 X.com 用户名和密码

### 推荐配置 2. ✅ 自动打开浏览器并登录

- **操作系统**: Windows 10/11, macOS, Linux3. ✅ 自动保存 cookies 到 `backend/data/x_cookies.json`

- **内存**: 至少 4GB 可用内存 4. ✅ 完成！

- **磁盘**: 至少 5GB 可用空间

### 方法 2: 使用环境变量

---

````bash

## 步骤 1: 获取 X Cookie# 1. 创建配置文件

cp .env.x.example .env.x

### 方法 A：使用浏览器开发者工具（推荐）

# 2. 编辑 .env.x，填写你的账号密码

1. **登录 X（Twitter）**X_USERNAME=your_email@example.com

   - 打开浏览器访问 https://twitter.comX_PASSWORD=your_password

   - 使用您的账号登录

# 3. 加载环境变量并运行

2. **打开开发者工具**# Windows PowerShell:

   - Windows/Linux: 按 `F12` 或 `Ctrl + Shift + I`Get-Content .env.x | ForEach-Object { $var = $_.Split('='); [Environment]::SetEnvironmentVariable($var[0], $var[1]) }

   - macOS: 按 `Cmd + Option + I`cd backend

python local_setup_auth.py

3. **获取 Cookie**

   - 切换到 `Application` 标签页（Chrome）或 `Storage` 标签页（Firefox）# 或者直接在Docker中运行：

   - 左侧菜单选择 `Cookies` → `https://twitter.com`docker-compose exec -e X_USERNAME=your_email -e X_PASSWORD=your_pass backend python manage.py setup_x_auth

   - 找到以下两个关键 Cookie：```



     | Cookie 名称 | 说明 | 示例值 |## 📝 示例输出

     |------------|------|--------|

     | `auth_token` | 认证令牌（必需） | `1a2b3c4d5e6f7g8h...` |```

     | `ct0` | CSRF 令牌（必需） | `9i8h7g6f5e4d3c2b...` |============================================================

Windows本地 - X.com自动登录认证

   - 双击 Cookie 值进行复制============================================================



4. **保存 Cookie 值**这个脚本会：

   - 将 `auth_token` 和 `ct0` 的值记录下来1. 自动打开浏览器并登录X.com

   - 这两个值将在后续配置中使用2. 自动填写你的账号密码

3. 保存cookies到 backend/data/x_cookies.json

### 方法 B：使用浏览器插件4. Docker会自动使用这个文件



1. 安装 Cookie 导出插件（任选其一）：============================================================

   - [EditThisCookie](https://chrome.google.com/webstore/detail/editthiscookie/fngmhnnpilhplaeedifhccceomclgfbg)（Chrome）

   - [Cookie-Editor](https://addons.mozilla.org/en-US/firefox/addon/cookie-editor/)（Firefox）请输入你的X.com账号信息：

（密码不会显示，输入后按Enter）

2. 登录 X（Twitter）后点击插件图标

用户名（邮箱/手机/用户名）: myemail@example.com

3. 找到 `auth_token` 和 `ct0` 并复制其值密码: ********



---是否显示浏览器窗口？[Y/n]: y



## 步骤 2: 克隆项目开始登录...



```bash============================================================

# 克隆仓库X.com 自动登录认证设置

git clone https://github.com/cyohei9907/auto-ski-info-subscribe.git============================================================



# 进入项目目录用户名: myemail@example.com

cd auto-ski-info-subscribe密码: ********

```模式: 显示浏览器



---正在访问X.com登录页面...

等待登录表单加载...

## 步骤 3: 配置环境变量输入用户名: myemail@example.com

点击下一步...

### 创建后端配置文件等待密码输入框...

输入密码...

```bash点击登录...

# 复制环境变量模板

cp backend/.env.example backend/.env等待登录完成...

```当前URL: https://x.com/home



### 编辑 `backend/.env` 文件验证登录状态...



使用任意文本编辑器打开 `backend/.env`，填入以下配置：保存cookies...



```ini✓ Cookies已保存到: E:\workspace\...\backend\data\x_cookies.json

# ===== X Cookie 认证（必需） =====  共 23 个cookies

# 从步骤 1 中获取的 Cookie 值

X_COOKIE_AUTH_TOKEN=粘贴你的_auth_token_值✓ 登录成功！

X_COOKIE_CT0=粘贴你的_ct0_值

下一步：

# ===== AI 服务（可选） =====1. 在docker-compose.yml中设置: USE_AUTHENTICATED_SCRAPER: 'True'

# 如果需要 AI 分析功能，填入 Gemini API Key2. 重启服务: docker-compose restart

# 获取地址：https://makersuite.google.com/app/apikey3. 测试: docker-compose exec backend python test_authenticated_scraper.py

AI_API_KEY_GOOGLE=your_gemini_api_key_here

============================================================

# ===== 数据库配置 =====✓ 认证设置完成！

# 本地开发使用 SQLite，无需修改============================================================

USE_CLOUD_SQL=False```

DATABASE_URL=sqlite:///data/db.sqlite3

## ⚙️ 启用认证爬虫

# ===== Django 配置 =====

DEBUG=True编辑 `docker-compose.yml`：

SECRET_KEY=your-local-dev-secret-key-change-in-production

ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0```yaml

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000services:

```  backend:

    environment:

**配置说明**：      USE_AUTHENTICATED_SCRAPER: "True" # 添加这行

- `X_COOKIE_AUTH_TOKEN`: X 的 `auth_token` Cookie 值（必需）      # ... 其他配置

- `X_COOKIE_CT0`: X 的 `ct0` Cookie 值（必需）

- `AI_API_KEY_GOOGLE`: Google Gemini API Key（可选，不填则 AI 功能不可用）  celery:

    environment:

### 创建前端配置文件（可选）      USE_AUTHENTICATED_SCRAPER: "True" # 添加这行

      # ... 其他配置

```bash

# 如需自定义前端配置  celery-beat:

cp frontend/.env.example frontend/.env    environment:

```      USE_AUTHENTICATED_SCRAPER: "True" # 添加这行

      # ... 其他配置

默认配置已足够，通常无需修改。```



---重启服务：



## 步骤 4: 启动服务```bash

docker-compose restart

### 使用 Docker Compose（推荐）```



```bash## ✅ 测试效果

# 构建并启动所有服务

docker-compose up -d```bash

# 测试认证爬虫

# 查看启动日志docker-compose exec backend python test_authenticated_scraper.py

docker-compose logs -f

# 预期输出：

# 等待所有服务就绪（约 30-60 秒）# ============================================================

# 看到类似 "Application startup complete" 表示启动成功# 结果: 获取到 25 条推文

```# ============================================================

#

**启动的服务包括**：# 最近的推文：

- `frontend` - React 前端（端口 3000）#

- `backend` - Django 后端（端口 8000）# 1. [2025-11-06 22:30:00+00:00] (ID: 1854321...)

- `redis` - Redis 消息队列（端口 6379）#    今日の雪情報です...

- `celery` - Celery 任务执行器#    💬 5 🔄 12 ❤️ 34

- `celery-beat` - Celery 定时调度器#

# ✓ 成功！找到 3 条今天的推文

### 验证服务状态```



```bash## ❓ 常见问题

# 查看所有容器状态

docker-compose ps### Q: 需要两步验证怎么办？



# 应该看到所有服务状态为 "Up"A: 脚本会自动检测并提示：

````

````

---⚠️ 需要两步验证

请在浏览器中完成验证，完成后按Enter...

## 步骤 5: 访问应用```



### 前端界面在浏览器中输入验证码，然后回到终端按 Enter 即可。



浏览器打开：**http://localhost:3000**### Q: 显示"未找到用户名输入框"？



- 首次访问会看到登录/注册页面A: X.com 有时会更改页面结构。如果遇到此问题：

- 点击"注册"创建新账号

- 注册后自动登录1. 确保网络连接正常

2. 尝试显示浏览器窗口（选择 Y）以查看实际情况

### 后端 API3. 如果页面加载异常，等待几秒后重试



浏览器打开：**http://localhost:8000**### Q: 登录失败？



- **API 文档**: http://localhost:8000/swagger/**可能原因**：

- **管理后台**: http://localhost:8000/admin/

  - 用户名: `admin`- ❌ 用户名或密码错误

  - 密码: `admin@123`- ❌ 账号被锁定或需要验证

- ❌ X.com 检测到自动化行为

---- ❌ 网络问题



## 步骤 6: 添加监控账号**解决方案**：



### 通过前端界面添加1. 检查账号密码是否正确

2. 在浏览器中手动登录一次确认账号正常

1. 访问 http://localhost:3000 并登录3. 使用显示浏览器模式（选择 Y）查看具体错误

4. 如果遇到额外验证，脚本会暂停让你完成

2. 点击左侧菜单的"账号管理"

### Q: "需要额外验证（电话/邮箱）"？

3. 点击右上角"添加账号"按钮

A: 这是 X.com 的安全机制，在新环境登录时触发：

4. 填写表单：

   - **X 用户名**: 目标账号的 username（例如 `elonmusk`，不含 `@`）```

   - **显示名称**: 自定义备注名称（例如 "Elon Musk 官方账号"）⚠️ X.com要求额外验证（电话/邮箱）

   - **启用监控**: 勾选此项以启动自动抓取这通常发生在新环境登录时



5. 点击"确定"保存请在浏览器中完成验证，然后按Enter继续...

````

### 通过管理后台添加

按照提示在浏览器中完成验证即可。

1. 访问 http://localhost:8000/admin/

### Q: Cookies 多久过期？

2. 登录管理员账号（`admin` / `admin@123`）

A: 通常 30-90 天。过期后重新运行 `python backend/local_setup_auth.py` 即可。

3. 点击 `X monitor` → `X accounts`

## 🔒 安全提示

4. 点击右上角"Add X account"

1. ⚠️ **不要提交包含密码的文件到 Git**

1. 填写：

   - **Username**: X 账号的 username（不含 `@`） - `.env.x` 已加入 `.gitignore`

   - **Display name**: 显示名称 - `x_cookies.json` 已加入 `.gitignore`

   - **Owner**: 选择归属用户

   - **Is active**: 勾选以启用自动监控 2. ✅ **建议使用专用账号**

1. 点击"Save" - 创建一个专门用于爬虫的 X 账号

   - 不要使用主账号

---

3. 🔐 **保护 cookies 文件**

## 步骤 7: 查看抓取结果 - `backend/data/x_cookies.json` 包含登录凭证

- 不要分享或上传此文件

### 自动抓取

## 📊 效果对比

系统会每 **15 分钟** 自动抓取已启用账号的最新推文。

| 项目 | 登录前 | 登录后 |

### 手动触发抓取| ---------------- | ----------- | ----------- |

| 可见推文数 | 4-6 条 | 20-100+条 |

#### 方法 1：前端界面| 最新推文 | ❌ 无法获取 | ✅ 实时获取 |

| "最新 10 条"按钮 | 返回 0 条 | ✅ 正常工作 |

1. 进入"账号管理"页面| 2025-11-06 推文 | ❌ 看不到 | ✅ 可以看到 |

2. 找到目标账号，点击"立即监控"按钮

3. 等待几秒后刷新"推文列表"页面## 🎯 下一步

#### 方法 2：API 调用 1. ✅ 运行 `python backend/local_setup_auth.py` 完成登录

2. ✅ 修改 `docker-compose.yml` 启用认证爬虫

```bash3. ✅ 重启服务并测试

# 获取账号列表4. ✅ 在前端点击"最新 10 条"按钮验证效果

curl http://localhost:8000/api/monitor/accounts/

## 🆘 需要帮助？

# 手动触发监控（替换 {account_id} 为实际 ID）

curl -X POST http://localhost:8000/api/monitor/accounts/{account_id}/monitor/如果遇到问题，请提供：

```

1. 完整的错误输出

### 查看推文 2. 是否显示了浏览器窗口

3. 浏览器中看到的页面内容

1. 点击左侧菜单的"推文列表"4. X.com 账号是否正常（能否手动登录）

1. 可以看到所有已抓取的推文
1. 点击推文卡片查看详情和 AI 分析结果

---

## 🎉 完成！

现在您已经成功部署了 Auto Ski Info Subscribe 系统！

### 下一步建议

- ✅ **添加更多监控账号** - 在"账号管理"页面添加您感兴趣的 X 账号
- ✅ **查看监控日志** - 在"监控日志"页面查看抓取历史和错误信息
- ✅ **配置 AI 分析** - 如尚未配置，添加 Gemini API Key 启用 AI 功能
- ✅ **调整监控频率** - 编辑 `backend/auto_ski_info/celery.py` 修改抓取间隔
- ✅ **部署到云端** - 参考 [DEPLOYMENT_OPTIMIZATION.md](DEPLOYMENT_OPTIMIZATION.md) 部署到 Google Cloud Run

---

## 🔧 常见问题

### Q1: Cookie 认证失败怎么办？

**A**:

1. 确认 Cookie 未过期（重新从浏览器获取）
2. 检查 `.env` 文件中的 `X_COOKIE_AUTH_TOKEN` 和 `X_COOKIE_CT0` 是否正确
3. 确保 Cookie 值中没有多余的空格或引号
4. 重启服务：`docker-compose restart backend celery`

### Q2: 无法抓取推文

**A**:

1. 确认目标账号为公开账号（私密账号需 Cookie 账号已关注）
2. 检查用户名拼写（不含 `@` 符号）
3. 查看后端日志：`docker-compose logs backend | grep -i error`
4. 尝试手动触发抓取并观察日志

### Q3: 前端无法连接后端

**A**:

1. 确认后端服务正在运行：`docker-compose ps backend`
2. 检查端口占用：`netstat -an | findstr :8000`
3. 查看 CORS 配置：`.env` 中的 `CORS_ALLOWED_ORIGINS` 应包含 `http://localhost:3000`
4. 重启所有服务：`docker-compose restart`

### Q4: Celery 定时任务不执行

**A**:

1. 检查 Redis 服务：`docker-compose ps redis`
2. 查看 Celery 日志：`docker-compose logs celery celery-beat`
3. 确认账号已启用：在管理后台检查 `is_active` 字段
4. 手动测试任务：
   ```bash
   docker-compose exec backend python manage.py shell
   >>> from x_monitor.tasks import monitor_all_active_accounts
   >>> monitor_all_active_accounts.delay()
   ```

### Q5: Docker 启动失败

**A**:

1. 确认 Docker Desktop 正在运行
2. 检查端口占用（3000, 8000, 6379）
3. 清理并重建：
   ```bash
   docker-compose down -v
   docker-compose build --no-cache
   docker-compose up -d
   ```

---

## 📚 更多资源

- **完整文档**: [README.md](README.md)
- **Cloud Run 部署**: [DEPLOYMENT_OPTIMIZATION.md](DEPLOYMENT_OPTIMIZATION.md)
- **Docker 调试**: [DOCKER_DEBUG.md](DOCKER_DEBUG.md)
- **VS Code 调试**: [VSCODE_DEBUG.md](VSCODE_DEBUG.md)
- **Windows 本地开发**: [LOCAL_DEV_WINDOWS.md](LOCAL_DEV_WINDOWS.md)

---

## 💬 获取帮助

遇到问题？欢迎：

- 📝 [提交 Issue](https://github.com/cyohei9907/auto-ski-info-subscribe/issues)
- 💬 [参与讨论](https://github.com/cyohei9907/auto-ski-info-subscribe/discussions)
- 📧 联系作者：cyohei9907@example.com

---

**祝使用愉快！🎿**
